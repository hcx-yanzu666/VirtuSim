# VirtuSim 开发阶段总结

本文档用于记录 VirtuSim 项目的阶段性成果。

每完成一个明确阶段，就新增一个“阶段 N”章节。阶段编号不等同于原开发计划的 Week 编号；每个阶段需要注明自己覆盖的原计划 Week 区间。

## 当前总体进度判断

当前成果整体相当于原计划 **Week 3 末**。

但需要准确区分：

- 原计划 Week 1 的 UE5 项目创建和 C++ 工程骨架已完成。
- 原计划 Week 1 的仓储场景搭建尚未完成。
- 原计划 Week 2 的 RobotActor、本地运动和机器人状态尚未完成。
- 原计划 Week 3 的 TempoROS 接入和跨主机 ROS2 双向通信已完成。
- 原计划 Week 4 的 `/cmd_vel` 控制机器人运动尚未开始。

因此，当前不是“Week 1～3 所有任务全部完成”，而是已经完成了项目基础和 Week 3 的核心通信验收。

---

## 阶段 1：UE5 工程基础与 TempoROS 双向通信

### 1. 阶段数

阶段 1

### 2. 对应原目标 Week 区间

Week 1～Week 3

### 3. 本阶段学习内容

#### UE5 C++ 工程

- UE5 C++ 项目的基本目录结构。
- `.uproject`、`Build.cs` 和模块依赖的作用。
- UE C++ 类、UHT、`UCLASS`、`UFUNCTION`、`UPROPERTY` 和 `GENERATED_BODY`。
- UnrealBuildTool 的编译和链接过程。
- Live Coding 对 UE C++ 编译的影响。

#### UE Subsystem

- `UWorldSubsystem` 的用途。
- World 子系统的初始化和销毁生命周期。
- `Initialize` 和 `Deinitialize` 的职责。
- `ShouldCreateSubsystem` 对子系统创建时机的控制。
- 为什么 ROS 通讯服务适合放在独立 Subsystem，而不是直接写进机器人 Actor。

#### TempoROS 和 ROS2

- ROS2 Node、Publisher、Subscriber 和 Topic 的关系。
- TempoROS 的 `UTempoROSNode::Create` API。
- TempoROS 的 `AddPublisher`、`AddSubscription` 和 `Publish` API。
- `FString` 与 `std_msgs/msg/String` 的自动转换。
- ROS2 QoS 的基本概念。
- ROS2 Domain ID、DDS 自动发现和跨主机通信。
- UE World Tick 与 `rclcpp::spin_some` 的关系。

#### 构建和运行时问题定位

- 编译错误、链接错误和运行时 DLL 错误的区别。
- 通过 `.rsp` 文件判断链接库是否进入最终目标。
- Windows DLL 搜索路径和依赖链。
- `rclcpp::init` 失败与 C++ 异常导致 UE 崩溃的关系。
- OpenSSL 1.1 DLL 缺失导致的 `LoadLibrary error 126`。

#### Git 和子模块

- Git submodule 的基本工作方式。
- 父仓库和子模块仓库分别提交。
- 将 TempoROS fork 到个人仓库。
- 修改 `.gitmodules` 和子模块 remote。
- 为什么子模块提交必须存在于远程仓库中。

### 4. 需要重点记忆

#### 第一重点：数据流

```text
ROS2 Publisher
      ↓
DDS
      ↓
Topic
      ↓
TempoROS Subscriber
      ↓
rclcpp::spin_some
      ↓
UE 回调函数
```

UE 发布方向则相反：

```text
蓝图或 UE C++
      ↓
URosCommunicationSubsystem
      ↓
UTempoROSNode::Publish
      ↓
TempoROS 类型转换
      ↓
DDS
      ↓
ROS2 Subscriber
```

#### 第二重点：TempoROS API 的职责

```cpp
UTempoROSNode::Create(...)
```

创建一个 TempoROS 节点。

```cpp
RosNode->AddPublisher<MessageType>(...)
```

创建一个指定消息类型的 Publisher。

```cpp
RosNode->AddSubscription<MessageType>(...)
```

注册一个 Subscriber 和对应回调。

```cpp
RosNode->Publish<MessageType>(...)
```

向 Topic 发布消息。

#### 第三重点：不要混淆对象和变量

```cpp
UTempoROSNode* RosNode;
```

`UTempoROSNode` 是类型，`RosNode` 是指针变量。

```cpp
RosNode->Publish(...)
```

表示通过 `RosNode` 指针调用 `UTempoROSNode` 的成员函数。

#### 第四重点：UE 编辑器 World 和 PIE World

编辑器打开地图时，World 可能只是编辑器 World。

点击 Play 后，UE 会创建 PIE World。

ROS 通信节点应该只在真正运行的 World 中创建：

```cpp
return World != nullptr && World->IsGameWorld();
```

否则打开 UE 编辑器时就可能启动 ROS 节点，导致编辑器启动阶段崩溃或产生无效通信。

#### 第五重点：双向通信验收不能只看一边

必须分别验证：

```text
ROS2 → UE
UE → ROS2
```

只看到 Topic 存在，不能证明消息回调已经工作。

### 5. 本阶段实现内容

#### UE 项目

- 创建 UE 5.6 C++ 项目 `VirtuSim`。
- 使用 VS2022 成功编译 `VirtuSimEditor`。
- 启用 TempoROS 插件。
- 配置 TempoROS 第三方依赖。
- 修复 `rclcpp` 外部库没有进入最终链接的问题。

#### ROS 通信子系统

新增：

```text
Source/VirtuSim/Communication/RosCommunicationSubsystem.h
Source/VirtuSim/Communication/RosCommunicationSubsystem.cpp
```

实现：

- 创建 `URosCommunicationSubsystem`。
- 继承 `UWorldSubsystem`。
- PIE 运行时自动创建 ROS2 Node。
- 节点名为 `virtusim_probe`。
- 创建测试 Topic `/virtusim/test`。
- 创建 FString Publisher。
- 创建 FString Subscriber。
- 提供蓝图函数 `PublishTestMessage`。
- 收到消息时输出 UE 日志。
- 发布失败时输出错误日志。

#### TempoROS 运行时修复

- 防止 ROS2 未初始化时继续创建节点。
- 防止编辑器 World 创建 ROS 通信子系统。
- 将需要的 OpenSSL DLL 目录加入 Windows 运行时搜索路径。
- 修复 `rmw_fastrtps_cpp.dll` 依赖加载问题。

#### 蓝图测试

创建测试蓝图：

```text
BP_RosPublishTest
```

蓝图执行流程：

```text
Event BeginPlay
      ↓
Delay 1 秒
      ↓
Get Ros Communication Subsystem
      ↓
Publish Test Message
```

测试消息：

```text
hello from unreal
```

#### 跨主机 ROS2 验收

环境：

```text
Windows UE5：172.16.86.59
VMware Ubuntu ROS2：172.16.84.244
ROS2：Humble
ROS_DOMAIN_ID：0
```

已验证：

```text
VMware ROS2 → UE Subscriber
UE Publisher → VMware ROS2
```

UE 已成功收到：

```text
hello from vmware
```

ROS2 已成功收到：

```yaml
data: hello from unreal
```

已确认：

- `ros2 node list` 可以发现 `/virtusim_probe`。
- `ros2 topic list -t` 可以发现 `/virtusim/test`。
- Topic 类型为 `std_msgs/msg/String`。
- UE Publisher 和 Subscriber 已被 DDS 正确发现。

#### Git

- VirtuSim 父仓库已推送到个人 GitHub。
- TempoROS 已 fork 到个人 GitHub。
- 子模块地址已改为：

```text
https://github.com/hcx-yanzu666/TempoROS.git
```

- 父仓库和 TempoROS 子模块均已完成提交和推送。

### 6. 本阶段验收结论

本阶段通信目标已完成：

```text
[x] UE5 项目可以编译
[x] TempoROS 可以加载
[x] UE 可以创建 ROS2 Node
[x] UE 可以创建 Publisher
[x] UE 可以创建 Subscriber
[x] VMware ROS2 可以发现 UE Node
[x] ROS2 → UE 消息接收成功
[x] UE → ROS2 消息发送成功
```

本阶段尚未完成：

```text
[ ] 仓储场景
[ ] RobotActor
[ ] UE 本地机器人运动
[ ] /cmd_vel
[ ] /odom
[ ] /tf
[ ] /scan
[ ] Nav2 NavigateToPose
```

---

## 下一阶段预告

### 阶段 2：RobotActor 与本地运动控制

对应原计划：

```text
Week 1 未完成部分
Week 2
```

目标：

- 创建基础仓储场景。
- 创建 `RobotActor`。
- 创建机器人车体和安装点。
- 创建本地运动状态。
- 不依赖 ROS2，通过 UE 输入控制机器人前进、后退和旋转。

验收标准：

```text
UE 中存在一个机器人
      ↓
输入线速度和角速度
      ↓
机器人在场景中运动
      ↓
输出位置、朝向、线速度和角速度
```

完成阶段 2 后，再进入：

```text
阶段 3：/cmd_vel 控制 UE 机器人
```

---

## 后续阶段记录模板

## 阶段 N：阶段名称

### 1. 阶段数

阶段 N

### 2. 对应原目标 Week 区间

Week X～Week Y

### 3. 学习内容

- 

### 4. 需要重点记忆

- 

### 5. 实现内容

- 

### 6. 验收方式

- 

### 7. 尚未完成和问题

- 

### 8. 下一阶段

- 
